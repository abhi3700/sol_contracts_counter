{
    "scripts": {
        "balance": "solana balance",
        "build": "anchor build",
        "pgid": "solana address -k target/deploy/counter-keypair.json",
        "deploylocal": "anchor deploy --provider.cluster localnet",
        "deploydev": "anchor deploy --provider.cluster devnet",
        "deploymain": "anchor deploy --provider.cluster mainnet"
    },
    "dependencies": {
        "@project-serum/anchor": "^0.20.1"
    },
    "devDependencies": {
        "chai": "^4.3.4",
        "mocha": "^9.0.3",
        "ts-mocha": "^8.0.0",
        "@types/mocha": "^9.0.0",
        "typescript": "^4.3.5"
    }
}
